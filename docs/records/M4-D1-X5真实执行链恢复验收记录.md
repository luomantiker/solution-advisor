# M4-D1：X5 真实执行链恢复验收记录

## 范围与安全边界

- 执行 Host 使用单一通用 HostAgent `x5-j6-host`；X5 身份来自已审核的
  `X5 / 1.0.0` Catalog 与 Binding，不由 Agent 本地配置自行声明。
- 受控镜像 digest 为
  `sha256:e9230ca9c1b65e4688f6103dad547b0785f75cd1ecbb536e7d939a7188419593`，工具链为
  `hb_mapper 1.24.3`。
- 板端预检确认 Runtime `1.24.5`、`aarch64` 和 BPU 访问可用。地址、账户、认证材料和 Host Key
  仅保留在 Agent Host 的受保护配置，不写入本记录、Git、数据库、页面或报告。
- 仅执行固定 Runner 和固定 `hrt_model_exec perf`；未接收 Shell、Docker 参数、宿主机路径、板卡地址或凭据。

## 恢复动作

1. 历史 `x5-a` Agent 已离线，原 Binding 已暂停并保留审计；创建
   `x5-j6-host → X5 / 1.0.0` 的单并发 HEALTHY Binding，动态 Worker 为 READY。
2. 修复 systemd `PrivateTmp=true` 与 Docker 挂载的边界：Agent 使用 Host 管理的 `work_root`，
   不再把 Runner 输入放在 Docker 守护进程不可见的私有 `/tmp`。
3. 修复 `x5-hrt-profile-1.0`：Runtime 1.24.x 的 `profiler.log` 中 JSON 片段被纳入固定解析器；
   原始 `profiler.log`、`profiler.csv`、预检、加载、推理日志和 `result.json` 均作为 Evidence 保留。

## 实际验证

| 阶段 | 任务 | 结果 |
| --- | --- | --- |
| 真实 X5 编译 | `task_c91a939ce2444299855b7feb10a80821` | `SUCCEEDED`，由现役 READY Worker 领取并产生受控 `model.bin`。 |
| 板端标准性能预设 | `task_cd7e98667b0e4fca8da3946bd4247a42` | `SUCCEEDED`，完成受控下发与一次固定 Runtime 调用。 |
| 性能 Evidence 解析 | 同上 | `MEASURED`；FPS `385.74294090418147`，平均延迟 `2.570995569229126 ms`。 |

本次输入为已完成通用分析的 `googlenet.onnx` Profile。首次恢复测试任务
`task_fe6aa1192ae6404bb7b54f2fd5d1efa9` 因私有 `/tmp` 挂载不可见而失败；该任务保留为真实失败审计，
未覆盖、未删除，也未作为成功结论。

## 结论边界

本记录只证明特定镜像锁、固定 Runner、固定模型和固定 Runtime 条件下的编译与一次板端性能测量已发生。
它不验证业务精度、稳定性、功耗或部署推荐；这些字段必须继续显示为 `NOT_VERIFIED`，不得用于交付结论。
