# M5-A-R1：S100 固定 Runner、真实执行与平台接入验收记录

日期：2026-08-31

## 真实证据

| 阶段 | 实际结果 |
| --- | --- |
| 镜像锁 | `registry.d-robotics.cc/deliver/ai_toolchain_ubuntu_22_s100_s600_cpu:v3.7.0`；image ID `sha256:eb401fa186f8…`；RepoDigest `sha256:f14856f507…`；amd64/Linux，创建于 2025-12-17。 |
| 容器自检 | Python 3.10.12；`hb_compile` 3.5.3、HBDK 4.7.5、HMCT 2.6.5；`/usr/ucp/hrt_model_exec` 的 UCP 为 3.13.6。 |
| 失败 fixture | 通用 8×8 `minimal.onnx` 返回 255；真实诊断为 NV12 image_convert 宽度至少需要 16。该失败不作为成功制品。 |
| S100 fixture | 新增可复现 `s100_minimal_16x16.onnx`，SHA256 `77b64fce50ad42b13d8a6380c222e9cf44ef39c2ab9d893820a618d794d8dabe`。 |
| 固定 Runner 编译 | `s100-runner-1.0.0` 在锁定镜像内返回 0；产物 `model.hbm`，格式 `s100_hbm`，35,552 bytes，SHA256 `81c15d9935d317a9cb2e35932a43688214525bf27866493179290b2612f209a4`。 |
| 板端预检 | 已授权 S100 为 aarch64 Linux；`hrt_model_exec` 3.7.3；`/dev/bpu`、`/dev/bpu_core0` 可访问。 |
| 板端 perf | 固定 `hrt_model_exec perf --model_file …model.hbm --model_name model --profile_path …/profile` 返回 0，生成 `profiler.log`（SHA256 `0565d467…`）和 `profiler.csv`（SHA256 `78cda782…`）。 |

原始自检、编译和板端日志均在执行 Host 的受控临时工作目录产生，未包含凭据、地址或私有路径；正式运行时应上传为 INTERNAL Evidence。

## 实际差异与边界

S100 不是 X5：其编译工具是 `hb_compile`，制品是 `.hbm` / `s100_hbm`，不得复用 `hb_mapper`、`model.bin`、`x5_bin`、`x5-hrt-profile-1.0` 或 X5 性能解释。容器与板端 Runtime 版本也不同。

本次只验证版本化 fixture 的编译、加载与性能调用。输出一致性、业务精度、稳定性、功耗、客户模型兼容性和部署推荐均为 `NOT_VERIFIED`。

## 当前发布状态

`platform_packages/s100/` 与固定 Runner 已进入 Git 工作区，但当前 S100 Candidate 仍为 `PENDING_INTEGRATION / BLOCKED`：HostAgent 尚未以受保护配置安装并注册该 Release，控制面尚无 Candidate revision 绑定的真实 Evidence 回传。因此未创建 S100 Catalog、Binding、Worker，也不会使 S100 出现在用户评估可选平台中。这是预期门禁，不是失败伪装为成功。
