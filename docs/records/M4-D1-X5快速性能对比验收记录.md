# M4-D1 X5 快速性能对比验收记录

## 范围

M4-D1 不实施输出一致性或业务精度评测，复用 M4-B-R0 已验真的 X5 受控性能链路：编译日志、固定 `hrt_model_exec perf`、原始 profile Evidence 与 `x5-hrt-profile-1.0` 解析。M4-B-R0 的真实板端证据、Runtime 1.24.5 和性能测量见 `M4-B-R0-X5板端环境与最小推理冒烟验收记录.md`。

## 本轮收口

- 管理端将板端操作明确为“执行 X5 标准性能预设”。
- 任务页与 PDF 在原有性能事实后显示 `x5-performance-advice-1.0` 的证据约束建议。
- 建议区分 Runtime CPU 执行段和编译分配日志确认的模型 CPU 算子，且不产生精度或部署结论。
- 精度、稳定性、功耗与部署推荐继续为 `NOT_VERIFIED`。

## 自动化

- `uv run pytest -q`：通过。
- `npm --prefix frontend run build`：通过。
- `git diff --check`、开发/生产 Compose config：通过。
- 生产 Compose：新镜像构建、迁移成功，API、Web、common-analyzer、PostgreSQL、Redis 与 MinIO 正常运行；Web 健康检查通过。
- 浏览器：Playwright 实际访问生产 Web 管理页，页面正常渲染；生产报告 API 在没有可信身份时返回 `401 identity_required`，符合 fail-closed。
- PDF：既有 M4-B-R0 的真实板端报告/PDF 回归由该记录覆盖；本轮只改变性能建议展示，未伪造新的板端性能结果。

该记录不含板卡地址、凭据、远端路径或 Secret。
